version: "3"

name: "generative-calendar"
services:
  web-app:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "80:80"

  back-service:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8080:8080"